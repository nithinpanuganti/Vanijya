# ==============================================================================
# Vanijya Backend Environment Configuration Template
# Copy this file to `apps/backend/.env` and set your configuration values.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. MongoDB Database Connection (REQUIRED)
# ------------------------------------------------------------------------------
# Option A: Local MongoDB instance:
# MONGODB_URI=mongodb://127.0.0.1:27017/vanijya
#
# Option B: MongoDB Atlas (Cloud Managed Cluster):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/vanijya?retryWrites=true&w=majority
MONGODB_URI=

# ------------------------------------------------------------------------------
# 2. JWT & Security Configuration (REQUIRED)
# ------------------------------------------------------------------------------
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------------------------
# 3. Server Configuration (OPTIONAL)
# ------------------------------------------------------------------------------
PORT=4000
NODE_ENV=development
